#!/bin/sh

set -e

if [ "$1" = "install" ]; then
    dpkg-divert --package jak-systemd-secure-boot --divert /etc/kernel/postinst.d/dracut.SecureBoot --rename /etc/kernel/postinst.d/dracut
fi

#DEBHELPER#
